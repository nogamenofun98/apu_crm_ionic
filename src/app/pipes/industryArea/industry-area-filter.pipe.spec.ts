import {IndustryAreaFilterPipe} from './industry-area-filter.pipe';

describe('IndustryAreaFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new IndustryAreaFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
